# @portal-solutions/jade

## Description
A TypeScript workspace containing "jade-element" and "jade-js", likely a UI framework or component library.

## Goals
- [ ] Develop Jade UI components
- [ ] Provide JS runtime support

## Progress
- [ ] Workspace setup

## JIT host-property mappings

Jade's Rust JIT can compile against a consistently property-mangled tenant
host while retaining canonical direct-property output by default. The
consumer-owned tenant ABI, complete-mapping validation, and Tier 0/1/2/WASM
configuration are documented in
[`docs/jit-host-method-names.md`](docs/jit-host-method-names.md).

---
*AI assisted*
